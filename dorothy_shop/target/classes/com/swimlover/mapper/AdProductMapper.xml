<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.swimlover.mapper.AdProductMapper">

	<select id = "getCategoryList" resultType = "com.swimlover.domain.CategoryVO">
	
		SELECT
			CATE_CODE,
			CATE_CODE_PRT,
			CATE_NAME
		FROM
			CATEGORY_TBL
		WHERE
			CATE_CODE_PRT IS NULL
	
	</select>
	
	<select id = "getSubCategoryList" resultType="com.swimlover.domain.CategoryVO">
	
		SELECT
			CATE_CODE,
			CATE_CODE_PRT,
			CATE_NAME
		FROM
			CATEGORY_TBL
		WHERE
			CATE_CODE_PRT = #{cate_code}
	
	</select>
	
	<insert id = "productInsert">
	
		INSERT INTO PRODUCT_TBL
			(PDT_NUM, CATE_CODE, CATE_CODE_PRT, PDT_NAME, PDT_PRICE, PDT_DISCOUNT, PDT_COMPANY, PDT_DETAIL, PDT_IMG_FOLDER, PDT_IMG, PDT_AMOUNT, PDT_BUY)
		VALUES
			(SEQ_PRODUCT_NUM.NEXTVAL, #{cate_code}, #{cate_code_prt}, #{pdt_name}, #{pdt_price}, #{pdt_discount}, #{pdt_company}, #{pdt_detail}, #{pdt_img_folder}, #{pdt_img}, #{pdt_amount}, #{pdt_buy})
	
	</insert>

</mapper>